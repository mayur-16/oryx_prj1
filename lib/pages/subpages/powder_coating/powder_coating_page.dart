import 'package:flutter/material.dart';

class PowderCoatingPage extends StatelessWidget {
  const PowderCoatingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Powder Coating"),
        centerTitle: true,
      ),
    );
  }
}
