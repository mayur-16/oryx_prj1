import 'package:flutter/material.dart';

class WoodenCoatingPage extends StatelessWidget {
  const WoodenCoatingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Wooden Coating"),
        centerTitle: true,
      ),
    );
  }
}
