import 'package:flutter/material.dart';

class Payslipsmc extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange.shade600,
    );
  }
}
