import 'package:flutter/material.dart';

class RevenueStatementPage extends StatelessWidget {
  const RevenueStatementPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Revenue Statement"),
        centerTitle: true,),
    );
  }
}
