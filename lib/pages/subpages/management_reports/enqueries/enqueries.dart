import 'package:flutter/material.dart';

class EnqueriesPage extends StatelessWidget {
  const EnqueriesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Enqueries"),
        centerTitle: true,),
    );
  }
}
