import 'package:flutter/material.dart';

class CashOutFlowPage extends StatelessWidget {
  const CashOutFlowPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Cash Out Flow"),
        centerTitle: true,),
    );
  }
}
