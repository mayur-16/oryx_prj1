import 'package:flutter/material.dart';

class ManagementReportsPage extends StatelessWidget {
  const ManagementReportsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Management Reports"),
        centerTitle: true,
      ),
    );
  }
}
