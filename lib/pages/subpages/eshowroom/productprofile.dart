import 'package:flutter/material.dart';

class ProductprofileEshowroom extends StatelessWidget {
  const ProductprofileEshowroom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.teal.shade300,
    );
  }
}
