import 'package:flutter/material.dart';

class Deliveryeshowroom extends StatelessWidget {
  const Deliveryeshowroom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepOrange.shade300,
    );
  }
}
