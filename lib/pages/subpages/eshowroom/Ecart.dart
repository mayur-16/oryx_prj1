import 'package:flutter/material.dart';

class EcartPage extends StatelessWidget {
  const EcartPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("E-Cart"),),
    );
  }
}
