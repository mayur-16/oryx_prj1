import 'package:flutter/material.dart';

class KitchenFittingsPage extends StatelessWidget {
  const KitchenFittingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Kitchen Fittings"),
        centerTitle: true,
      ),
    );
  }
}
