import 'package:flutter/material.dart';

class OrdersNschedulesCrm extends StatelessWidget {
  const OrdersNschedulesCrm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red.shade300,
    );
  }
}
