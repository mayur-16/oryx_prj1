import 'package:flutter/material.dart';

class ApplyfordocHr extends StatelessWidget {
  const ApplyfordocHr({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
